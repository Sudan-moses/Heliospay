## Packages
date-fns | Formatting dates for payments and student records
recharts | Dashboard charts and payment trends

## Notes
- Assume Replit Auth handles `/api/login` and `/api/logout` endpoints
- Assume API schemas and routes are available at `@shared/schema` and `@shared/routes`
- Zod coercion is used in forms to safely parse numeric inputs for tuition and payment amounts
- Using window.print() combined with `@media print` CSS for receipt printing functionality
